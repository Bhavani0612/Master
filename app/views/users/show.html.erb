<!-- <h1>Users List</h1>


<%= form_tag(users_index_path, method: :get) do %>
  <p> Search for a user </p>
  <%= text_field_tag(:search, params[:search]) %>
  <%= submit_tag ("Search") %>
<% end %>


<table>
  <tr>
    <th>User Name </th>
    <th>User Email </th>
  </tr>
 <% @users.each do |user| %>
  <tr>
    <td><%= user.name%></td>
    <td><%= user.email%></td>
  </tr>
  <% end %>
 </table>



 -->
